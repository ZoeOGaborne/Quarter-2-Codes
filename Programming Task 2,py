def fee(distance, rate):
    tot_fee = distance * rate
    print(f"\nTotal Delivery Fee: ₱{(round(tot_fee, 2))}")

distance = (float(input("Enter distance in kilometers: ")))
rate = (float(input("Enter rate per kilometer (₱): ")))
fee(distance, rate)